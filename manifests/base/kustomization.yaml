apiVersion: kustomize.manifests.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: jupyter-kernel-controller
      app.kubernetes.io/name: jupyter-kernel-controller
      app.kubernetes.io/version: "1081000"
      app.kubernetes.io/managed-by: kubectl
    includeSelectors: false

resources:
  - ../crd/
  - ../rbac/
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml

images:
  - name: weekenthralling/jupyter-kernel-controller
    newTag: "1081000"
